# lefthook.yml

pre-commit:
  parallel: true
  jobs:
    - run: npx eslint --fix {staged_files}
      name: Lint staged files
      glob:
        - '*.js'
        - '*.jsx'
        - '*.ts'
        - '*.tsx'
      stage_fixed: true